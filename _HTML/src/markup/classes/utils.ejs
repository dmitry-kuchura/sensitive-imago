<%
	class Utils {
		setAttrs(attrs) {
			let array = [];
			Object.keys(attrs).forEach((entry) => {
				array.push(`${entry}=${attrs[entry]}`)
			});
			return array.join(' ');
		}
	};

	locals.Utils = new Utils();
%>
